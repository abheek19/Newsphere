# Render Blueprint Configuration
# Deploy NewsSphere in 1 click
services:
  - type: web
    name: newssphere-news-portal
    env: python
    buildCommand: ""
    startCommand: python3 app.py
    envVars:
      - key: PORT
        value: 10000
